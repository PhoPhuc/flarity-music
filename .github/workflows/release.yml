name: Release & Build Cross-Platform (Windows & macOS)

# Disabled: builds are done locally and uploaded via `gh release create`
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Manual trigger only
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is currently disabled. Builds are done locally."
